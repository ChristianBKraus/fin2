package fin2.configuration.organisation;
/*
import fin2.model.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RequestMapping(path = PlantController.PATH)
@RestController
public class PlantController {
    static final String PATH = "/api/plant";

    @Autowired
    PlantRepo repo;
    @Autowired
    PlantService service;

    @GetMapping("")
    public List<Plant> getAll() {
        return repo.findAll();
    }

    @PostMapping("")
    public Plant post(@RequestBody Plant plant ) throws Exception {
        return repo.save(service.post(plant,false));
    }

}


 */